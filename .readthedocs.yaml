version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: rtd/conf.py

python:
  install:
    - requirements: rtd/requirements.txt

# Default branch is master, not main
